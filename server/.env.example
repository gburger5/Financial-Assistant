PORT=3000
NODE_ENV=development
JWT_SECRET=                   # any random string for local dev
FRONTEND_URL=http://localhost:5173

# Email (Brevo) — required for password reset
BREVO_API_KEY=
EMAIL_FROM=
EMAIL_FROM_NAME=

# Plaid — required for bank account linking
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

AWS_REGION=us-east-1
